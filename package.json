{
  "name": "weavr-claw-agent",
  "version": "0.1.0",
  "description": "weavr wallet tool and approval plugin for a self-hosted Claw Agent (Hermes)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'",
    "sign-check": "node tools/sign-check.mjs",
    "gate-test": "python3 plugins/weavr-wallet-gate/test_gate.py"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  }
}
